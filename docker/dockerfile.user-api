FROM golang:1.20 as build
ENV CGO_ENABLED 0
ARG BUILD_ENV

WORKDIR /build
COPY . .
RUN cd services/user-api && go build -ldflags "-X main.build=${BUILD_ENV}"

# RELEASE
FROM alpine:3.14
ARG BUILD_DATE
ARG BUILD_ENV
WORKDIR /websvc
COPY --from=build /build/services/user-api .
CMD ["./user-api"]

LABEL org.opencontainers.image.created="${BUILD_DATE}" \
      org.opencontainers.image.title="user api service" \
      org.opencontainers.image.authors="Veerash Ayyagari <veerash@veerashayyagari.com>" \
      org.opencontainers.image.source="https://github.com/veerashayyagari/go-otel/services/user-api" \
      org.opencontainers.image.revision="${BUILD_ENV}" \
      org.opencontainers.image.vendor="Veerash Ayyagari"
      